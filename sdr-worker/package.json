{
  "name": "upixel-sdr-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "POC de worker SDR externo: consome whatsapp_message_queue (route='sdr'), gera resposta via LLM e responde pelo canal WhatsApp/Evolution do uPixel. Só conversa — não altera o CRM.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
